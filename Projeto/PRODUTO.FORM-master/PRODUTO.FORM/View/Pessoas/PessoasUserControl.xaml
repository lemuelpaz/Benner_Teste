<UserControl x:Class="PRODUTO.FORM.View.Pessoas.PessoasUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">

    <UserControl.Resources>
        <!-- Estilo padrão para botões -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#ec1444"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#f02470"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Botão secundário -->
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#e0e0e0"/>
            <Setter Property="Foreground" Value="#333"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#bdbdbd"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="#f9f9f9" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <!-- ================= FILTROS ================= -->
        <Border Background="White" CornerRadius="6" Padding="10" Margin="0 0 0 10" BorderBrush="#ddd" BorderThickness="1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" >
                <TextBlock Text="Nome:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Width="200" Margin="0,0,15,0" Text="{Binding FiltroNome, Mode=TwoWay}"/>
                <TextBlock Text="CPF:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Width="150" Margin="0,0,15,0" Text="{Binding FiltroCPF, Mode=TwoWay}"/>
                <Button Content="Pesquisar" Command="{Binding PessoaCommand}" CommandParameter="Pesquisar"/>
                <Button Content="Limpar" Style="{StaticResource SecondaryButton}" Command="{Binding PessoaCommand}" CommandParameter="Limpar"/>
            </StackPanel>
        </Border>

        <!-- ================= GRID DE PESSOAS ================= -->
        <Border Grid.Row="1" Background="White" CornerRadius="6" Padding="10" BorderBrush="#ddd" BorderThickness="1">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,10">
                    <Button Content="Incluir" Command="{Binding PessoaCommand}" CommandParameter="Incluir"/>
                    <Button Content="Salvar" Style="{StaticResource SecondaryButton}" Command="{Binding PessoaCommand}" CommandParameter="Salvar"/>
                    <Button Content="Editar" Style="{StaticResource SecondaryButton}" Command="{Binding PessoaCommand}" CommandParameter="Editar"/>
                    <Button Content="Excluir" Style="{StaticResource SecondaryButton}" Command="{Binding PessoaCommand}" CommandParameter="Excluir"/>
                </StackPanel>

                <DataGrid x:Name="dgPessoas"
                          ItemsSource="{Binding Pessoas}"
                          SelectedItem="{Binding PessoaSelecionada, Mode=TwoWay}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          HeadersVisibility="Column"
                          SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Header="Selecionar" Binding="{Binding IsSelected}" Width="80"/>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="200"/>
                        <DataGridTextColumn Header="CPF" Binding="{Binding CPF}" Width="150"/>
                        <DataGridTextColumn Header="Endereço" Binding="{Binding Endereco}" Width="250"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

        <!-- ================= GRID DE PEDIDOS ================= -->
        <Border Grid.Row="2" Background="White" CornerRadius="6" Padding="10" BorderBrush="#ddd" BorderThickness="1" Margin="0,10,0,0">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,10">
                    <CheckBox Content="Mostrar apenas entregues" Margin="0,0,15,0"/>
                    <CheckBox Content="Mostrar apenas pagos" Margin="0,0,15,0"/>
                    <CheckBox Content="Mostrar pendentes de pagamento"/>
                </StackPanel>

                <DataGrid ItemsSource="{Binding TodosPedidos}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          HeadersVisibility="Column"
                          SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Header="Selecionar" Binding="{Binding IsSelected}" Width="80"/>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                        <DataGridTextColumn Header="Data da Venda" Binding="{Binding DataVenda, StringFormat=dd/MM/yyyy}" Width="150"/>
                        <DataGridTextColumn Header="Valor Total" 
                    Binding="{Binding PrecoTotal, StringFormat={}{0:C2}, ConverterCulture=pt-BR}" 
                    Width="120"/>
                        <DataGridTextColumn Header="Forma de Pagamento" Binding="{Binding FormaPagamento}" Width="150"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120"/>
                        <DataGridTemplateColumn Header="Ações" Width="250">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Marcar Pago"
                                                Command="{Binding DataContext.MarcarPagoCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"/>
                                        <Button Content="Enviado" Style="{StaticResource SecondaryButton}"
                                                Command="{Binding DataContext.EnviadoCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"/>
                                        <Button Content="Recebido" Style="{StaticResource SecondaryButton}"
                                                Command="{Binding DataContext.RecebidoCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>

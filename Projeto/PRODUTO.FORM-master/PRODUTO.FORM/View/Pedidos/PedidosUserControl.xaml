<UserControl x:Class="PRODUTO.FORM.View.Pedidos.PedidosUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid Background="#F9F9F9" Margin="20">
        <Border CornerRadius="10" Background="White" Padding="20" BorderBrush="#DDD" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.2"/>
            </Border.Effect>

            <StackPanel Orientation="Vertical" VerticalAlignment="Top" HorizontalAlignment="Stretch">

                <!-- Título -->
                <TextBlock Text="Cadastro de Pedido" 
                           FontSize="20" FontWeight="Bold" 
                           Foreground="#333" Margin="0,0,0,10"/>

                <!-- Seleção de Cliente -->
                <!-- Seleção de Cliente -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,10">
                    <TextBlock Text="Cliente:" Width="120" VerticalAlignment="Center"/>
                    <ComboBox Width="300" Height="28"
              ItemsSource="{Binding Clientes}"
              DisplayMemberPath="Nome"
              SelectedItem="{Binding ClienteSelecionado, Mode=TwoWay}"/>
                </StackPanel>

                <!-- Seleção de Produto -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,10">
                    <TextBlock Text="Produto:" Width="120" VerticalAlignment="Center"/>
                    <ComboBox Width="300" Height="28"
              ItemsSource="{Binding Produtos}"
              DisplayMemberPath="Nome"
              SelectedItem="{Binding ProdutoSelecionado, Mode=TwoWay}"/>
                    <TextBlock Text="Qtd:" Margin="10,0,5,0" VerticalAlignment="Center"/>
                    <TextBox Width="60" Height="28" Text="{Binding QuantidadeProduto, Mode=TwoWay}"/>
                    <Button Content="Adicionar" Width="100" Margin="10,0,0,0"
            Command="{Binding PedidoCommand}" 
            CommandParameter="AdicionarProduto"/>
                </StackPanel>

                <!-- DataGrid Produtos -->
                <TextBlock Text="Produtos no Pedido" FontSize="16" FontWeight="SemiBold" Margin="0,10,0,5"/>
                <DataGrid ItemsSource="{Binding ItensPedido}"
                          AutoGenerateColumns="False" 
                          CanUserAddRows="False"
                          CanUserDeleteRows="True"
                          HeadersVisibility="Column"
                          Height="200" Margin="0,0,0,10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Código" Binding="{Binding Codigo}" Width="100"/>
                        <DataGridTextColumn Header="Descrição" Binding="{Binding Nome}" Width="*"/>
                        <DataGridTextColumn Header="Quantidade" Binding="{Binding Quantidade}" Width="100"/>
                        <DataGridTextColumn Header="Preço Unitário" Binding="{Binding Preco}" Width="120"/>
                        <DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal}" Width="120" IsReadOnly="True"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Valor Total -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <TextBlock Text="Valor Total: " FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding ValorTotal}" FontSize="16" 
                               FontWeight="Bold" Foreground="#ec1444" VerticalAlignment="Center" />
                </StackPanel>

                <!-- Forma de Pagamento -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,15,0,0">
                    <TextBlock Text="Forma de Pagamento:" Width="150" VerticalAlignment="Center"/>
                    <ComboBox Width="200" Height="28" SelectedItem="{Binding FormaPagamento}">
                        <ComboBoxItem Content="Dinheiro"/>
                        <ComboBoxItem Content="Cartão de Crédito"/>
                        <ComboBoxItem Content="Cartão de Débito"/>
                        <ComboBoxItem Content="Pix"/>
                    </ComboBox>
                </StackPanel>

                <!-- Botão Finalizar -->
                <Button Content="Finalizar Pedido" 
                        Width="180" Height="40" 
                        Background="#ec1444" Foreground="White" 
                        FontWeight="Bold" Margin="0,20,0,0"
                        HorizontalAlignment="Right"
                        Command="{Binding PedidoCommand}" CommandParameter="FinalizarPedido"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
